Duplicate Detection Assignment Solution

Starting with cleaning the names of the companies to avoid little differences. Following by a lookup of duplicates by pandas method on the column names, 
and then using two copies of the duplicated values to get the index and the columns on which this values are duplicated.
